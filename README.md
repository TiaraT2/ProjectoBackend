Implementación de sessions, storage y cookies para crear usuarios, entrar y cerrar sesion del mismo mediante los metodos:
POST /register
POST /login
POST /signout

También se añadieron vistas para los mismo.